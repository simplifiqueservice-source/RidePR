# RidePR Admin MVP

Painel web estatico para testar rapidamente o backend RidePR.

Abra `index.html` no navegador, informe a `API baseUrl`, faca login como admin e use os botoes de listagem/fluxo de corrida.

Nao ha build nem dependencias npm neste MVP.
