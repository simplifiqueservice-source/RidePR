# RidePR Admin MVP

Painel web estatico para testar rapidamente o backend RidePR.

Abra `index.html` no navegador, faca login como admin e use os botoes de listagem/fluxo de corrida. O endereco da API fica configurado internamente no painel.

Nao ha build nem dependencias npm neste MVP.
